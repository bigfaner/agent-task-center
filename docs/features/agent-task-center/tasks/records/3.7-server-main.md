# Task Record: Server main.go (wire up + embed.FS)

## Summary
Implement server cmd/server/main.go with full dependency injection wiring, chi router with CORS middleware, SPA fallback handler, and embed.FS for web UI static files. Created web/embed.go package for embed directive accessible from cmd/server. 10 tests covering CORS middleware, SPA handler (root, SPA routes, static assets, non-GET), and static FS helper.

## Status
- Status: completed
- Started: 2026-04-14 01:47
- Completed: 2026-04-14 01:54
- Time Spent: ~7m

## Files
- Created: - server/cmd/server/main_test.go
- server/cmd/server/testdata/spa/index.html
- server/cmd/server/testdata/spa/assets/app.js
- server/web/embed.go
- Modified: - server/cmd/server/main.go

## Key Decisions
- Created separate web/embed.go package for embed.FS since go:embed paths are relative to source file and cmd/server/ cannot reach web/dist
- SPA fallback reads index.html directly from fs.FS instead of using http.FileServer to avoid 301 redirect issues
- CORS middleware uses wildcard origin (*) for development mode with OPTIONS preflight support
- newStaticFS falls back to embedded FS when STATIC_DIR is empty or directory doesn't contain index.html

## Testing
- Tests Passed: 10
- Tests Failed: 0
- Coverage: 53.8%

## Acceptance Criteria
- [x] go run ./cmd/server starts successfully, listens on :8080
- [x] GET /api/projects returns JSON (empty list)
- [x] GET / returns React app index.html
- [x] SPA routes (e.g. GET /projects/1) return index.html (not 404)
- [x] CORS headers correctly set (dev mode allows all origins)

## Notes
无
